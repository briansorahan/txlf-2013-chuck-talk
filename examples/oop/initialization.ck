SinOsc @ isNull;
SinOsc notNull;

if (isNull == null) {
    chout <= "isNull is null" <= IO.newline();
} else {
    chout <= "isNull is not null" <= IO.newline();
}

if (notNull == null) {
    chout <= "notNull is null" <= IO.newline();
} else {
    chout <= "notNull is not null" <= IO.newline();
}

